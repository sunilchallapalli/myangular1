import { PamodelDirective } from './pamodel.directive';

describe('PamodelDirective', () => {
  it('should create an instance', () => {
    const directive = new PamodelDirective();
    expect(directive).toBeTruthy();
  });
});
