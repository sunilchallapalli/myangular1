import { MyAttrDirectiveDirective } from './my-attr-directive.directive';

describe('MyAttrDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAttrDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
