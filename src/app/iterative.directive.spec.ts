import { IterativeDirective } from './iterative.directive';

describe('IterativeDirective', () => {
  it('should create an instance', () => {
    const directive = new IterativeDirective();
    expect(directive).toBeTruthy();
  });
});
